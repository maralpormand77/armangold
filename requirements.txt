# نیازمندی‌های سبک و استاندارد پروژه (کاملاً سازگار با پلن‌های رایگان Render، Railway و PythonAnywhere)
# تمامی ماژول‌های شبکه عصبی LSTM، بانک داده SQLite و سرور وب با کتابخانه‌های استاندارد و پرسرعت پایتون پیاده‌سازی شده‌اند.
requests>=2.28.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
